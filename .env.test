# Test database configuration
# Used by integration tests with Testcontainers (auto-provisioned)
# or local PostgreSQL for manual test runs
PGHOST=localhost
PGPORT=55432
PGDATABASE=valence_test
PGUSER=postgres
PGPASSWORD=postgres
